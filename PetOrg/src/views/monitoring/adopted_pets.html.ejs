<div class="index_c">
  <h1>Mascotas adoptadas: <%= adopted_pets.length %>%></h1>
  <div class="button_panel">
      <button>
          <a class="link" href="<%= indexPath %>">Lista de acciones</a>
      </button>
  </div>
  
  <table class="ind_table">
      <thead>
      <tr>
          <th>Mascota</th>
          <th>Dueño</th>
          <th></th>
      </tr>
      </thead>
      <tbody>
      <% adopted_pets.forEach(adopted_pet => {%>
          <tr>
              <td>
                <button>
                  <a class="link" href="<%= petPath(adopted_pet.id) %>"><%= adopted_pet.name %></a>
                </button>
              <td>
                  <button>
                      <a class="link" href="<%= userPath(adopted_pet.user.id) %>"><%= adopted_pet.user.name %></a>
                  </button>
              </td>
              <td>
                  <button>
                      <a class="link" href="<%= monitoringPath(adopted_pet.id) %>">Seguimiento</a>
                  </button>
              </td>
          </tr>
      <% }) %>
      </tbody>
  </table>
</div>
